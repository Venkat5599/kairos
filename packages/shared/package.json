{
  "name": "@kairos/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for IntentFlow",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest"
  },
  "keywords": ["intentflow", "types", "shared"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "ethers": "^6.10.0"
  }
}
